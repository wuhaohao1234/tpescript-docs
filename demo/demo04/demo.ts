/** 
 * 「类型 + 方括号」表示法
    let fibonacci: number[] = [1, 1, 2, 3, 5];
 * 数组泛型
    let fibonacci: Array<number> = [1, 1, 2, 3, 5];
 * 接口
    interface NumArr {
        [index: number]: number;
    }
    let arrNum: NumArr = [1, 2, 3, 4, 5]
 */